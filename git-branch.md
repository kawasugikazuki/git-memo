# git branchについて
gitのブランチに関するコマンド
```
git branch 
```
現在いるブランチを確認することができる
```
git branch -d <ブランチ名> 
```
ブランチを消すことができる
```
git branch -b  <ブランチ名> <ブランチ名>
```
ブランチ全体の差分を比較できる
```
git branch <ブランチ名>
```
ブランチを作成することだけできる
```
git branch -m  <ブランチ名>
```
現在のブランチの名前を変えることができる